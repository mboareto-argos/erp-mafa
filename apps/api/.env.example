# Usuário de aplicação sem BYPASSRLS: usado em runtime.
DATABASE_URL="postgresql://erp_mafa_app:erp_mafa_app@localhost:5432/erp_mafa?schema=public"
# Credencial administrativa: somente migrations e operações de manutenção.
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/erp_mafa?schema=public"
PORT=3001

# Segredo de assinatura JWT — gerar um valor proprio por ambiente, nunca
# reaproveitar (docs/architecture/overview.md, secao 9.3).
JWT_SECRET="replace-with-a-random-secret"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="30d"
JWT_PREAUTH_EXPIRES_IN="5m"
